import { InputNumbersDirective } from './input-numbers.directive';

describe('InputNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new InputNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
