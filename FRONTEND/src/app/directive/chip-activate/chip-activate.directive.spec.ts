import { ChipActivateDirective } from './chip-activate.directive';

describe('ChipActivateDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipActivateDirective();
    expect(directive).toBeTruthy();
  });
});
